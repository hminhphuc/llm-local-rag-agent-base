# Core — Module 1 (Local LLM qua Ollama)
ollama>=0.4.0
openai>=1.50.0          # demo API tương thích OpenAI (1_ollama_basics/03_openai_compat.py)
python-dotenv>=1.0.0

# Notebook (Module 1 chạy từng cell)
jupyterlab>=4.0.0
ipykernel>=6.0.0

# Module 2 (RAG) dùng GIAO DIỆN Open WebUI qua docker-compose.yml — KHÔNG cần thư viện Python.
